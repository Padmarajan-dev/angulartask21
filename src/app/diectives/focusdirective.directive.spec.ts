import { FocusdirectiveDirective } from './focusdirective.directive';

describe('FocusdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
